<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# configapi

```go
import "wintergate/api/config"
```

## Index

- [Constants](<#constants>)
- [Variables](<#variables>)
- [type AuthSection](<#AuthSection>)
- [type Endpoint](<#Endpoint>)
- [type Handler](<#Handler>)
  - [func NewHandler\(registerer \*Registerer\) \(\*Handler, error\)](<#NewHandler>)
  - [func \(h \*Handler\) PutSnapshot\(ctx \*gin.Context\)](<#Handler.PutSnapshot>)
  - [func \(h \*Handler\) RegisterRoutes\(router gin.IRouter\)](<#Handler.RegisterRoutes>)
- [type ProtectedEndpoint](<#ProtectedEndpoint>)
- [type RateLimitEndpoint](<#RateLimitEndpoint>)
- [type Registerer](<#Registerer>)
  - [func NewRegisterer\(\) \*Registerer](<#NewRegisterer>)
  - [func \(r \*Registerer\) AuthRegistry\(\) \*authconfig.Registry](<#Registerer.AuthRegistry>)
  - [func \(r \*Registerer\) Register\(snapshot Snapshot\) error](<#Registerer.Register>)
  - [func \(r \*Registerer\) RouteRegistry\(\) \*routeconfig.Registry](<#Registerer.RouteRegistry>)
- [type RoutesSection](<#RoutesSection>)
- [type Snapshot](<#Snapshot>)
- [type TimeWindow](<#TimeWindow>)


## Constants

<a name="DefaultRoute"></a>

```go
const (
    // DefaultRoute 설정 정보를 수신하는 기본 API 경로입니다.
    DefaultRoute = "/api/config"
)
```

## Variables

<a name="ErrInvalidSnapshot"></a>

```go
var (
    ErrInvalidSnapshot = errors.New("invalid snapshot")
    ErrNilRegisterer   = errors.New("nil registerer")
)
```

<a name="AuthSection"></a>
## type AuthSection

AuthSection 인증 관련 설정 섹션입니다.

```go
type AuthSection struct {
    JWTAlgorithm string          `json:"jwt_algorithm"`
    JWTAudience  string          `json:"jwt_audience"`
    JWTClockSkew string          `json:"jwt_clock_skew"`
    JWTIssuer    string          `json:"jwt_issuer"`
    JWTSecret    string          `json:"jwt_secret,omitempty"`
    JWKS         json.RawMessage `json:"jwks"`
}
```

<a name="Endpoint"></a>
## type Endpoint

Endpoint 라우트 매칭에 필요한 기본 엔드포인트 정보를 표현합니다.

```go
type Endpoint struct {
    Path    string `json:"path"`
    Method  string `json:"method"`
    Service string `json:"service"`
}
```

<a name="Handler"></a>
## type Handler

Handler 외부 설정 스냅샷을 수신해 내부 레지스트리에 반영합니다.

```go
type Handler struct {
    // contains filtered or unexported fields
}
```

<a name="NewHandler"></a>
### func NewHandler

```go
func NewHandler(registerer *Registerer) (*Handler, error)
```

NewHandler 설정 수신 Handler를 생성합니다.

<a name="Handler.PutSnapshot"></a>
### func \(\*Handler\) PutSnapshot

```go
func (h *Handler) PutSnapshot(ctx *gin.Context)
```

PutSnapshot 전달받은 설정 스냅샷을 내부 저장소에 반영합니다.

<a name="Handler.RegisterRoutes"></a>
### func \(\*Handler\) RegisterRoutes

```go
func (h *Handler) RegisterRoutes(router gin.IRouter)
```

RegisterRoutes 설정 수신 엔드포인트를 Gin 라우터에 등록합니다.

<a name="ProtectedEndpoint"></a>
## type ProtectedEndpoint

ProtectedEndpoint 추가 인가 조건이 필요한 엔드포인트를 표현합니다.

```go
type ProtectedEndpoint struct {
    Endpoint
    Roles      []string    `json:"roles"`
    TimeWindow *TimeWindow `json:"time_window,omitempty"`
}
```

<a name="RateLimitEndpoint"></a>
## type RateLimitEndpoint

RateLimitEndpoint 요청 제한 규칙이 적용되는 엔드포인트를 표현합니다.

```go
type RateLimitEndpoint struct {
    Endpoint
    Roles    []string `json:"roles"`
    Duration string   `json:"duration"`
    Limit    int      `json:"limit"`
}
```

<a name="Registerer"></a>
## type Registerer

Registerer 설정 스냅샷을 받아 필요한 내부 저장소에 일괄 등록합니다.

```go
type Registerer struct {
    // contains filtered or unexported fields
}
```

<a name="NewRegisterer"></a>
### func NewRegisterer

```go
func NewRegisterer() *Registerer
```

NewRegisterer 설정 스냅샷 등록용 Registerer를 생성합니다.

<a name="Registerer.AuthRegistry"></a>
### func \(\*Registerer\) AuthRegistry

```go
func (r *Registerer) AuthRegistry() *authconfig.Registry
```

AuthRegistry 인증 런타임 설정 저장소를 반환합니다.

<a name="Registerer.Register"></a>
### func \(\*Registerer\) Register

```go
func (r *Registerer) Register(snapshot Snapshot) error
```

Register 설정 스냅샷 전체를 내부 저장소에 반영합니다.

<a name="Registerer.RouteRegistry"></a>
### func \(\*Registerer\) RouteRegistry

```go
func (r *Registerer) RouteRegistry() *routeconfig.Registry
```

RouteRegistry 보호 라우트 런타임 설정 저장소를 반환합니다.

<a name="RoutesSection"></a>
## type RoutesSection

RoutesSection 보호 엔드포인트 설정을 보관합니다.

```go
type RoutesSection struct {
    Protected []ProtectedEndpoint
}
```

<a name="Snapshot"></a>
## type Snapshot

Snapshot 외부에서 전달하는 Wintergate 설정 스냅샷입니다.

```go
type Snapshot struct {
    Auth      *AuthSection        `json:"auth,omitempty"`
    Routes    *RoutesSection      `json:"routes,omitempty"`
    RateLimit []RateLimitEndpoint `json:"rate_limit,omitempty"`
}
```

<a name="TimeWindow"></a>
## type TimeWindow

TimeWindow 엔드포인트 접근 가능 시간을 표현합니다.

```go
type TimeWindow struct {
    Start    string `json:"start"`
    End      string `json:"end"`
    Timezone string `json:"timezone"`
}
```

# gatewayapi

```go
import "wintergate/api/gateway"
```

## Index

- [type Handler](<#Handler>)
  - [func NewHandler\(orchestrator \*internalgateway.Orchestrator\) \*Handler](<#NewHandler>)
  - [func \(h \*Handler\) Receive\(ctx \*gin.Context\)](<#Handler.Receive>)
  - [func \(h \*Handler\) RegisterRoutes\(router \*gin.Engine\)](<#Handler.RegisterRoutes>)
- [type ReceiveResponse](<#ReceiveResponse>)


<a name="Handler"></a>
## type Handler

Handler 게이트웨이의 공통 트래픽 수신 진입점을 담당합니다.

```go
type Handler struct {
    // contains filtered or unexported fields
}
```

<a name="NewHandler"></a>
### func NewHandler

```go
func NewHandler(orchestrator *internalgateway.Orchestrator) *Handler
```

NewHandler 생성된 Orchestrator를 주입받아 게이트웨이 Handler를 생성합니다.

<a name="Handler.Receive"></a>
### func \(\*Handler\) Receive

```go
func (h *Handler) Receive(ctx *gin.Context)
```

Receive 등록되지 않은 모든 외부 요청을 게이트웨이 수준에서 수신합니다.

<a name="Handler.RegisterRoutes"></a>
### func \(\*Handler\) RegisterRoutes

```go
func (h *Handler) RegisterRoutes(router *gin.Engine)
```

RegisterRoutes 게이트웨이 트래픽 수신 진입점을 Gin 엔진의 기본 라우트로 등록합니다.

<a name="ReceiveResponse"></a>
## type ReceiveResponse

ReceiveResponse 게이트웨이가 트래픽을 수신했음을 반환하는 응답 본문입니다.

```go
type ReceiveResponse struct {
    Received bool   `json:"received"`
    Method   string `json:"method"`
    Path     string `json:"path"`
}
```

# response

```go
import "wintergate/api/response"
```

## Index

- [type APIResponse](<#APIResponse>)


<a name="APIResponse"></a>
## type APIResponse

APIResponse 모든 HTTP API가 공통으로 사용하는 응답 본문입니다.

```go
type APIResponse struct {
    Success bool   `json:"success"`
    Message string `json:"message,omitempty"`
    Data    any    `json:"data,omitempty"`
}
```

# cmd

```go
import "wintergate/cmd"
```

## Index



# auth

```go
import "wintergate/internal/auth"
```

## Index

- [Variables](<#variables>)
- [func BearerToken\(authorizationHeader string\) \(string, error\)](<#BearerToken>)
- [type Claims](<#Claims>)
- [type Decoder](<#Decoder>)
  - [func NewDecoder\(registry \*authconfig.Registry\) \*Decoder](<#NewDecoder>)
  - [func \(d \*Decoder\) Decode\(token string\) \(Claims, error\)](<#Decoder.Decode>)
  - [func \(d \*Decoder\) ReplaceRegistry\(registry \*authconfig.Registry\) error](<#Decoder.ReplaceRegistry>)


## Variables

<a name="ErrConfigUnavailable"></a>

```go
var (
    ErrConfigUnavailable          = errors.New("config unavailable")
    ErrInvalidAudience            = errors.New("invalid audience")
    ErrInvalidAuthorizationHeader = errors.New("invalid authorization header")
    ErrInvalidIssuer              = errors.New("invalid issuer")
    ErrInvalidSignature           = errors.New("invalid signature")
    ErrInvalidToken               = errors.New("invalid token")
    ErrNilRegistry                = errors.New("nil registry")
    ErrTokenExpired               = errors.New("token expired")
    ErrTokenNotYetValid           = errors.New("token not yet valid")
    ErrUnsupportedAlgorithm       = errors.New("unsupported algorithm")
)
```

<a name="BearerToken"></a>
## func BearerToken

```go
func BearerToken(authorizationHeader string) (string, error)
```

BearerToken Authorization 헤더에서 Bearer 토큰 값을 추출합니다.

<a name="Claims"></a>
## type Claims

Claims JWT에서 추출한 표준 claims와 원본 payload를 함께 보관합니다.

```go
type Claims struct {
    Subject   string
    Issuer    string
    Audience  []string
    Roles     []string
    ExpiresAt time.Time
    IssuedAt  time.Time
    NotBefore time.Time
    Raw       map[string]any
}
```

<a name="Decoder"></a>
## type Decoder

Decoder 인증 설정을 바탕으로 JWT를 검증하고 claims를 추출합니다.

```go
type Decoder struct {
    // contains filtered or unexported fields
}
```

<a name="NewDecoder"></a>
### func NewDecoder

```go
func NewDecoder(registry *authconfig.Registry) *Decoder
```

NewDecoder JWT 검증용 Decoder를 생성합니다.

<a name="Decoder.Decode"></a>
### func \(\*Decoder\) Decode

```go
func (d *Decoder) Decode(token string) (Claims, error)
```

Decode JWT의 서명과 표준 claims를 검증한 뒤 결과를 반환합니다.

<a name="Decoder.ReplaceRegistry"></a>
### func \(\*Decoder\) ReplaceRegistry

```go
func (d *Decoder) ReplaceRegistry(registry *authconfig.Registry) error
```

ReplaceRegistry Decoder가 사용할 인증 설정 저장소를 교체합니다.

# gateway

```go
import "wintergate/internal/gateway"
```

## Index

- [Variables](<#variables>)
- [type AuthenticateTask](<#AuthenticateTask>)
  - [func NewAuthenticateTask\(decoder TokenDecoder\) \*AuthenticateTask](<#NewAuthenticateTask>)
  - [func \(t \*AuthenticateTask\) Run\(\_ context.Context, state \*State\) error](<#AuthenticateTask.Run>)
- [type Orchestrator](<#Orchestrator>)
  - [func NewOrchestrator\(tasks ...Task\) \*Orchestrator](<#NewOrchestrator>)
  - [func \(o \*Orchestrator\) Receive\(ctx context.Context, request Request\) error](<#Orchestrator.Receive>)
- [type Request](<#Request>)
- [type RouteTask](<#RouteTask>)
  - [func NewRouteTask\(router \*internalroute.Router, decoder \*internalauth.Decoder\) \*RouteTask](<#NewRouteTask>)
  - [func \(t \*RouteTask\) Run\(ctx context.Context, state \*State\) error](<#RouteTask.Run>)
- [type State](<#State>)
- [type Task](<#Task>)
- [type TokenDecoder](<#TokenDecoder>)


## Variables

<a name="ErrInvalidRequest"></a>

```go
var (
    ErrInvalidRequest  = errors.New("invalid request")
    ErrNilTask         = errors.New("nil task")
    ErrNilTokenDecoder = errors.New("nil token decoder")
)
```

<a name="AuthenticateTask"></a>
## type AuthenticateTask

AuthenticateTask 게이트웨이 요청의 Bearer JWT를 검증합니다.

```go
type AuthenticateTask struct {
    // contains filtered or unexported fields
}
```

<a name="NewAuthenticateTask"></a>
### func NewAuthenticateTask

```go
func NewAuthenticateTask(decoder TokenDecoder) *AuthenticateTask
```

NewAuthenticateTask Bearer JWT 검증용 AuthenticateTask를 생성합니다.

<a name="AuthenticateTask.Run"></a>
### func \(\*AuthenticateTask\) Run

```go
func (t *AuthenticateTask) Run(_ context.Context, state *State) error
```

Run Authorization 헤더가 있을 때 Bearer JWT를 검증하고 claims를 상태에 기록합니다.

<a name="Orchestrator"></a>
## type Orchestrator

Orchestrator 게이트웨이 요청 처리 작업을 순차적으로 조율합니다.

```go
type Orchestrator struct {
    // contains filtered or unexported fields
}
```

<a name="NewOrchestrator"></a>
### func NewOrchestrator

```go
func NewOrchestrator(tasks ...Task) *Orchestrator
```

NewOrchestrator 게이트웨이 요청 처리용 Orchestrator를 생성합니다.

<a name="Orchestrator.Receive"></a>
### func \(\*Orchestrator\) Receive

```go
func (o *Orchestrator) Receive(ctx context.Context, request Request) error
```

Receive 게이트웨이로 들어온 요청에 대해 등록된 작업을 순차 실행합니다.

<a name="Request"></a>
## type Request

Request 게이트웨이가 수신한 요청의 핵심 정보만 분리해 전달합니다.

```go
type Request struct {
    Service             string
    Method              string
    Path                string
    AuthorizationHeader string
}
```

<a name="RouteTask"></a>
## type RouteTask



```go
type RouteTask struct {
    // contains filtered or unexported fields
}
```

<a name="NewRouteTask"></a>
### func NewRouteTask

```go
func NewRouteTask(router *internalroute.Router, decoder *internalauth.Decoder) *RouteTask
```



<a name="RouteTask.Run"></a>
### func \(\*RouteTask\) Run

```go
func (t *RouteTask) Run(ctx context.Context, state *State) error
```



<a name="State"></a>
## type State

State 오케스트레이터가 각 작업 사이에서 공유하는 요청 처리 상태입니다.

```go
type State struct {
    Request Request
    Claims  *internalauth.Claims
}
```

<a name="Task"></a>
## type Task

Task 게이트웨이 요청 처리 중 개별 작업 단위를 정의합니다.

```go
type Task interface {
    Run(ctx context.Context, state *State) error
}
```

<a name="TokenDecoder"></a>
## type TokenDecoder

TokenDecoder Bearer 토큰을 검증하고 claims를 반환하는 계약입니다.

```go
type TokenDecoder interface {
    Decode(token string) (internalauth.Claims, error)
}
```

# route

```go
import "wintergate/internal/route"
```

## Index

- [Variables](<#variables>)
- [type Router](<#Router>)
  - [func NewRouter\(registry \*routeconfig.Registry\) \*Router](<#NewRouter>)
  - [func \(r \*Router\) ReplaceRegistry\(registry \*routeconfig.Registry\) error](<#Router.ReplaceRegistry>)
  - [func \(r \*Router\) Route\(service string\) \(\[\]routeconfig.RouteInfo, error\)](<#Router.Route>)


## Variables

<a name="ErrServiceNotFound"></a>

```go
var (
    ErrServiceNotFound = errors.New("cannot find service")
    ErrNilRegistry     = errors.New("registry is required")
)
```

<a name="Router"></a>
## type Router

Router 서비스 이름으로 등록된 라우팅 정보를 조회합니다.

```go
type Router struct {
    // contains filtered or unexported fields
}
```

<a name="NewRouter"></a>
### func NewRouter

```go
func NewRouter(registry *routeconfig.Registry) *Router
```

NewRouter 라우팅 정보 조회용 Router를 생성합니다.

<a name="Router.ReplaceRegistry"></a>
### func \(\*Router\) ReplaceRegistry

```go
func (r *Router) ReplaceRegistry(registry *routeconfig.Registry) error
```

ReplaceRegistry Router가 사용할 라우팅 저장소를 교체합니다.

<a name="Router.Route"></a>
### func \(\*Router\) Route

```go
func (r *Router) Route(service string) ([]routeconfig.RouteInfo, error)
```

Route 서비스에 속한 라우팅 정보 목록을 반환합니다.

# config

```go
import "wintergate/internal/auth/config"
```

## Index

- [Variables](<#variables>)
- [type Registry](<#Registry>)
  - [func NewRegistry\(\) \*Registry](<#NewRegistry>)
  - [func \(r \*Registry\) PublicKey\(kid string\) \(\*rsa.PublicKey, error\)](<#Registry.PublicKey>)
  - [func \(r \*Registry\) Register\(cfg RuntimeConfig\) error](<#Registry.Register>)
  - [func \(r \*Registry\) Snapshot\(\) \(RuntimeConfig, bool\)](<#Registry.Snapshot>)
- [type RuntimeConfig](<#RuntimeConfig>)


## Variables

<a name="ErrInvalidConfig"></a>

```go
var (
    ErrInvalidConfig     = errors.New("invalid config")
    ErrInvalidKeyID      = errors.New("invalid key id")
    ErrInvalidKeySet     = errors.New("invalid key set")
    ErrKeyNotFound       = errors.New("key not found")
    ErrKeySetUnavailable = errors.New("key set unavailable")
)
```

<a name="Registry"></a>
## type Registry

Registry 인증 런타임 설정과 JWKS를 메모리에 보관합니다.

```go
type Registry struct {
    // contains filtered or unexported fields
}
```

<a name="NewRegistry"></a>
### func NewRegistry

```go
func NewRegistry() *Registry
```

NewRegistry 빈 인증 설정 Registry를 생성합니다.

<a name="Registry.PublicKey"></a>
### func \(\*Registry\) PublicKey

```go
func (r *Registry) PublicKey(kid string) (*rsa.PublicKey, error)
```

PublicKey 주어진 kid에 해당하는 RSA 공개키를 반환합니다.

<a name="Registry.Register"></a>
### func \(\*Registry\) Register

```go
func (r *Registry) Register(cfg RuntimeConfig) error
```

Register 전달받은 인증 런타임 설정과 JWKS로 현재 값을 교체합니다.

<a name="Registry.Snapshot"></a>
### func \(\*Registry\) Snapshot

```go
func (r *Registry) Snapshot() (RuntimeConfig, bool)
```

Snapshot 현재 인증 런타임 설정의 사본을 반환합니다.

<a name="RuntimeConfig"></a>
## type RuntimeConfig

RuntimeConfig 인증 런타임 설정과 JWKS 값을 보관합니다.

```go
type RuntimeConfig struct {
    JWTAlgorithm string
    JWTAudience  string
    JWTClockSkew time.Duration
    JWTIssuer    string
    JWTSecret    []byte
    JWKS         []byte
}
```

# config

```go
import "wintergate/internal/route/config"
```

## Index

- [Variables](<#variables>)
- [type Entry](<#Entry>)
- [type Registry](<#Registry>)
  - [func NewRegistry\(\) \*Registry](<#NewRegistry>)
  - [func \(r \*Registry\) Register\(cfg RuntimeConfig\) error](<#Registry.Register>)
  - [func \(r \*Registry\) RouteInfos\(service string\) \(\[\]RouteInfo, bool\)](<#Registry.RouteInfos>)
- [type RegistryRouteInfo](<#RegistryRouteInfo>)
- [type RouteInfo](<#RouteInfo>)
- [type RuntimeConfig](<#RuntimeConfig>)


## Variables

<a name="ErrInvalidConfig"></a>

```go
var ErrInvalidConfig = errors.New("invalid config")
```

<a name="Entry"></a>
## type Entry

Entry 하나의 URL 경로와 대상 서비스 매핑을 표현합니다.

```go
type Entry struct {
    Path       string
    Service    string
    HttpMethod string
    Roles      []string
}
```

<a name="Registry"></a>
## type Registry

Registry 라우팅 런타임 설정과 엔트리를 메모리에 보관합니다.

```go
type Registry struct {
    // contains filtered or unexported fields
}
```

<a name="NewRegistry"></a>
### func NewRegistry

```go
func NewRegistry() *Registry
```

NewRegistry 빈 라우팅 설정 Registry를 생성합니다.

<a name="Registry.Register"></a>
### func \(\*Registry\) Register

```go
func (r *Registry) Register(cfg RuntimeConfig) error
```

Register 전달받은 라우팅 설정과 엔트리로 현재 값을 교체합니다.

<a name="Registry.RouteInfos"></a>
### func \(\*Registry\) RouteInfos

```go
func (r *Registry) RouteInfos(service string) ([]RouteInfo, bool)
```

RouteInfos 지정한 서비스에 대응하는 라우팅 정보 목록을 반환합니다.

<a name="RegistryRouteInfo"></a>
## type RegistryRouteInfo

RegistryRouteInfo 하나의 서비스에 속한 라우팅 정보를 표현합니다.

```go
type RegistryRouteInfo struct {
    Path       string
    HttpMethod string
    Roles      []string
}
```

<a name="RouteInfo"></a>
## type RouteInfo

RouteInfo 외부로 반환하는 라우팅 정보를 표현합니다.

```go
type RouteInfo struct {
    Path       string
    HttpMethod string
    Roles      []string
}
```

<a name="RuntimeConfig"></a>
## type RuntimeConfig

RuntimeConfig 라우팅 런타임 설정과 엔트리를 함께 보관합니다.

```go
type RuntimeConfig struct {
    Entries []Entry
}
```

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
